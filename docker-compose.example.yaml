version: '3.8'

services:
  udpxy:
    image: your-registry/udpxy:latest  # Replace with your registry address
    container_name: udpxy
    network_mode: host
    environment:
      - UDPXY_PORT=10011
      - UDPXY_VERBOSE=true
      - UDPXY_RENEW=300
    restart: unless-stopped
